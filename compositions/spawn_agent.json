{
  "description": "Spawn an agent worktree and harness process for a task",
  "version": "0.1.0",
  "root": {
    "op": "Sequential",
    "stages": [
      {
        "op": "Stage", "id": "STAGE:http_post",
        "_comment": "POST to caloron-shell /spawn endpoint with {sprint_id, task_id, agent_id, repo, worktree_base}"
      },
      {
        "op": "Stage", "id": "STAGE:kv_set",
        "_comment": "Store PID at caloron:{sprint_id}:agent:{agent_id}:pid"
      }
    ]
  }
}
